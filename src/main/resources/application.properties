server.port=8081

fuente.id=${FUENTE_ID:fuenteLocal}

# PostgreSQL Database Configuration
spring.datasource.url=${DB_URL:jdbc:postgresql://dpg-d2utat7fte5s73bi0db0-a.virginia-postgres.render.com:5432/fuentes_postgres_grupo4}
spring.datasource.username=${DB_USERNAME:fuentes_postgres_grupo4_user}
spring.datasource.password=${DB_PASSWORD:4zHA1Vl50PTP7PTyM8DRv36rUeUf5yLF}
spring.datasource.driver-class-name=org.postgresql.Driver

# Servicios externos
procesadorPdi.url=${PROCESADOR_PDI_URL:https://modulo-procesador-pdi.onrender.com}

# JPA/Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update

# Actuator Configuration
management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always

# DevTools Configuration
spring.devtools.restart.enabled=true

# DataDog
management.datadog.metrics.export.enabled=true
management.datadog.metrics.export.api-key=${DD_KEY:dfeb2a301dc03752994738fceb7420f2}
management.datadog.metrics.export.step=1m
management.datadog.metrics.export.host-tag=pdi-host
management.datadog.metrics.export.uri=https://api.us5.datadoghq.com

spring.application.name=${FUENTE_ID:fuente1}

# AMQP
app.hechos.updates.queue=${HECHOS_UPDATES_Q:hechos.updates.q}
app.hechos.new.fanout=${NEW_HECHOS_TOPIC:hechos.new}

spring.rabbitmq.host=${QUEUE_HOST:gorilla.lmq.cloudamqp.com}
spring.rabbitmq.username=${QUEUE_USERNAME:hbgwrrko}
spring.rabbitmq.password=${QUEUE_PASSWORD:YZN-Frd-DnrlnE4h2REvFoNZ9FeGhf_O}
spring.rabbitmq.virtual-host=${QUEUE_USERNAME:hbgwrrko}
spring.rabbitmq.port=5671
spring.rabbitmq.ssl.enabled=true
spring.rabbitmq.listener.simple.acknowledge-mode=MANUAL
spring.rabbitmq.listener.simple.concurrency=2
spring.rabbitmq.listener.simple.prefetch=20